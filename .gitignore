# Rust
target/

# Node / TypeScript
node_modules/
dist/

# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Local data / secrets
*.db
*.db-journal
*.db-wal
*.db-shm
.env
.env.*
data/
bae-examples.env
bae-config.toml
bae-setup.sh
# baectl build/ready/run local state — holds a plaintext client key
# (resolved.json) and possibly harness secrets (harness.env); same
# sensitivity class as .env above.
.baectl/

# awman workflow internals
.awman/workflows

# reference-assistant file-tool workspace (created at runtime by the example)
examples/reference-assistant/workspace/
