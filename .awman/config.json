{
  "agent": "claude",
  "dynamicWorkflows": {
    "agentsToModels": {
      "codex": [
        "gpt-5.5"
      ],
      "opencode": [
        "omlx/Qwen3.6-35B-A3B-UD-MLX-4bit"
      ],
      "claude": [
        "claude-sonnet-5",
        "claude-opus-4-8",
        "claude-fable-5"
      ]
    },
    "defaultLeader": "claude::claude-fable-5",
    "guidance": [
      "Attempt to split the workflow evenly among the available agent types. Do not use claude for everything, claude, codex, and opencode should get rougly even usage so long as their models are appropriate.",
      "Use Qwen 3.6 for simpler, well-scoped tasks like writing tests and docs",
      "Use Sonnet 5 for simpler, well-scoped tasks like writing pre-designed functions and modules",
      "Use Opus and GPT 5.5 evenlyfor more complex tasks like building large modules from scratch or complex refactors",
      "Use Fable only when there is truly complex or multi-part work that needs to be done, extremely difficult research, or coordination/planning for large groups of sub-agents.",
      "Emulate the BMAD method's 'adverserial review' strategy when configuring the workflow for complex implementations (docs.bmad-method.org/explanation/adversarial-review/)"
    ]
  }
}