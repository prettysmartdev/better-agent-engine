# @prettysmartdev/bae-max-web

Vite + React + TypeScript frontend for the MAX dashboard. Builds to a static
`dist/` served by `max/server`. See the root `max/Makefile` and repository
`README.md` for the standard `build`/`test`/`lint`/`fmt`/`clean` verbs.
